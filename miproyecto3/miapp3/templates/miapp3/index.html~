{% load staticfiles %}
<html>
	<head>
		<title>Tienda Virtual</title>
		<link rel="stylesheet" type="text/css" href="{% static "css/hojaCSS.css" %}">
	</head>
	<body>	
		<h1>TIENDA VIRTUAL</h1>
				{% if user.is_authenticated %}
					<p>Soy {{ user.get_username }}</p>
					<a href="{% url 'addarticulo' %}">Añadir Articulo</a>
				{% endif %}
		<nav>
			<ul>
				{% if user.is_authenticated %}	
					<li><a href="{% url 'logout' %}">Logout</a></li>
				{% else %}
					<li><a href="{% url 'registro' %}">Registro</a></li>
					<li><a href="{% url 'login' %}">Login</a></li>
				{% endif %}
			</ul>
		</nav>		
		
		{% if lista_Articulo %}
			<table border="2">
				<th>Todos los articulos</th>
				<tr>					
					<td>Nombre</td>
					<td>Fecha</td>
					<td>Precio</td>
					<td>Tipo</td>
					<td>Usuario</td>
					
				</tr>	<!--a continuacion un for que recorre lista_Articulo-->
				{% for articulo in lista_Articulo %}
				<tr>
					<td><a href="{% url 'detalle_articulo' articulo.id %}">{{ articulo.nombre }}</a></td>
					<td>{{ articulo.fecha }}</td>
					<td>{{ articulo.precio }}</td>
					<td>{{ articulo.tipo }}</td>
					<td>{{ articulo.user }}</td>
					
					
				</tr>
				{% endfor %}
			</table>
		{% else %}
			<p>No hay articulos para mostrar</p>			
		{% endif %}
		<a href="{% url 'NuevoUsado' 'U' %}">Solo articulos de segunda mano</a>	
		<a href="{% url 'NuevoUsado' 'N' %}">Solo articulos nuevos</a>
					
		
	</body>
</html>
